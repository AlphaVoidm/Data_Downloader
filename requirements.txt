# Core (required for the coverage engine, audit, acquisition, and dashboard)
pandas>=2.2
numpy>=1.26
requests>=2.32
pycountry>=24.6
python-dotenv>=1.0
streamlit>=1.39
openpyxl>=3.1

# Scientific geospatial extractor (ERA5 / CMIP6 via the CDS API) — optional.
# Without these, the ERA5/CDS connector degrades to DEPENDENCY_MISSING and the
# engine falls back to NASA POWER (open) for climate variables.
cdsapi>=0.7
xarray>=2024.1
pyarrow>=15.0        # Parquet output (falls back to CSV when absent)

# Raster zonal statistics (GPWv4 population extraction) — optional.
# Without rasterio, the GPWv4 connector degrades to DEPENDENCY_MISSING.
rasterio>=1.3
