# Copy values into your shell or a local .env file (gitignored). Never commit
# real credentials — this file is a template only.
#
# The keys that were previously committed here are COMPROMISED and have been
# rotated. Generate fresh keys from each provider and put them in `.env`
# (never in git) or in Streamlit secrets:
#   EIA      -> https://www.eia.gov/opendata/ (register for an API key)
#   ENTSO-E  -> https://transparency.entsoe.eu (RESTful API token)
#   Ember    -> https://ember-energy.org/data/api/
#   CDS      -> https://cds.climate.copernicus.eu/how-to-api (uid:key)
EIA_API_KEY=
ENTSOE_API_TOKEN=
EMBER_API_KEY=
CDS_API_KEY=
CDSAPI_URL=https://cds.climate.copernicus.eu/api
